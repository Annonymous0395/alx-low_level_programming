#include "main.h"
#include <stdio.h>

/**
 * main - Entry point
 * @argc: command line
 * @argv: array of argc
 *
 * Return: Always 0 (success)
 */

int main(int argc, char *argv[])
{(void) argv; /*ignore argv*/
	printf("%d", argc);
	return (0);
}
