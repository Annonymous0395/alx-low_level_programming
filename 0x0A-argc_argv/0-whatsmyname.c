#include <stdio.h>
#include "main.h"

/**
 * main - Entry point
 * @argc: number of command line args
 * @argv: array of argc
 *
 * Return: 0 (successful)
 */

int main(int argc __attribute__((unused)), char *argv[])
{
	printf("%s\n", argv[0]);

	return (0);
}
